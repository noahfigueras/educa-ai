import animate from "tw-animate-css";

/** @type {import('tailwindcss').Config} */
const config = {
  content: [/* your paths */],
  theme: {
    extend: {},
  },
  plugins: [animate],
};

export default config;
